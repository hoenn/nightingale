<h1>New Account</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_account_path(@conn, :create, current_user(@conn).id)) %>

<span><%= link "Back", to: Routes.user_account_path(@conn, :index, current_user(@conn).id) %></span>
